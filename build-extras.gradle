android {
    compileSdkVersion 24
    buildToolsVersion "25.0.1"
    packagingOptions {
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/NOTICE'
    }
}

dependencies {

}

ext.cdvCompileSdkVersion = 35 //Biometric Manager framework needs cdvCompileSdkVersion > 29
